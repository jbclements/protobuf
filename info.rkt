#lang setup/infotab

(define name
  "Protocol Buffers")
(define blurb
  '("Protocol Buffer serialization library"))
(define categories
  '(io))

(define version
  "1.0.0")

(define primary-file
  "main.rkt")
